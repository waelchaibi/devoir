version: '3'
services:
  backend:
    build:
      context: ./backend
      dockerfile: ../docker/dockerfile.backend
    ports:
      - "5000:5000"
  frontend:
    build:
      context: ./frontend
      dockerfile: ../docker/dockerfile.frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend